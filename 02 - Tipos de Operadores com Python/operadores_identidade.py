saldo = 1000
limite = 1000

print(saldo is limite) # Retorna True pois ambos ocupam a mesma região de memória

print(saldo is not limite) # Verifica se os valores NÃO ocupam a mesma região de memória, retornando False
